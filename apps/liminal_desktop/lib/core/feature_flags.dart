/// Product feature gates for the desktop shell (not user settings).
abstract final class LiminalFeatureFlags {
  /// Desktop widget windows (spawn_app / multi-window) — disabled until stable.
  static const bool desktopAppsEnabled = false;
}
